# 한글 떼기 놀이터 디자인 시스템

## 1. 분위기와 정체성

만 4세 전후의 아이가 혼자 눌러도 길을 잃지 않는 밝고 포근한 학습 놀이터다. 시그니처는 종이와 크레용을 연상시키는 따뜻한 바탕 위에, 한글 자소가 가장 또렷한 주인공으로 보이는 구성이다. 장식보다 선택과 피드백을 선명하게 보여 주며 보호자가 곁에서 보아도 차분한 화면을 지향한다.

## 2. 색상

| 역할 | 토큰 | 값 | 용도 |
|---|---|---|---|
| 바탕 | `--surface-canvas` | `#fffaf0` | 전체 배경 |
| 바탕/하늘 | `--surface-sky` | `#eaf7ff` | 홈 상단 분위기 |
| 표면 | `--surface-card` | `#ffffff` | 카드, 버튼, 캔버스 |
| 표면/보조 | `--surface-soft` | `#fff3d9` | 안내 및 비활성 영역 |
| 글자/기본 | `--text-primary` | `#29324a` | 제목, 글자 |
| 글자/보조 | `--text-secondary` | `#626b7e` | 설명, 안내 |
| 주황 | `--accent-orange` | `#f47b42` | 따라쓰기, 주요 동작 |
| 주황/진함 | `--accent-orange-strong` | `#d85b2b` | 대비가 필요한 글자 |
| 파랑 | `--accent-blue` | `#5488e8` | 소리 퀴즈, 언어 선택 |
| 보라 | `--accent-violet` | `#8068d8` | 파랑 계열 보조 |
| 분홍 | `--accent-pink` | `#ec6f91` | 진행, 축하 |
| 성공 | `--status-success` | `#2f9d72` | 정답과 완료 |
| 오류 | `--status-error` | `#c94a62` | 오답 피드백 |
| 포커스 | `--focus-ring` | `#315fc5` | 키보드 포커스 |
| 테두리 | `--border-soft` | `#dce4f1` | 구획과 캔버스 |

색상은 학습 상태를 설명할 때 사용한다. 본문은 항상 충분한 대비를 유지하고, 장식 색 위 텍스트는 `--text-primary` 또는 흰색 중 WCAG 2.2 AA를 만족하는 조합만 쓴다.

## 3. 타이포그래피

- 기본/표시 글꼴: Jua, Malgun Gothic, Apple SD Gothic Neo, sans-serif.
- 표시 제목: `clamp(2.5rem, 7vw, 4.75rem)`, 줄 높이 1.08.
- 화면 제목: `clamp(1.5rem, 4vw, 2rem)`, 줄 높이 1.2.
- 카드 제목: `clamp(1.65rem, 3vw, 2.2rem)`, 줄 높이 1.25.
- 본문: `1rem` 이상, 줄 높이 1.55.
- 보조 문구: `0.95rem` 이상, 줄 높이 1.5.
- 아이가 선택하는 글자는 일반 UI 텍스트보다 크게 표시하되 컨테이너 밖으로 잘리지 않게 `clamp()`를 사용한다.

## 4. 간격과 레이아웃

- 기본 단위는 4px이다.
- 간격 토큰: `--space-1` 4px, `--space-2` 8px, `--space-3` 12px, `--space-4` 16px, `--space-5` 20px, `--space-6` 24px, `--space-8` 32px, `--space-10` 40px, `--space-12` 48px, `--space-16` 64px.
- 최대 콘텐츠 폭은 홈 1100px, 학습 화면 760px이다.
- 모바일 여백은 16px, 태블릿 이상은 24px이다.
- 375px에서는 모든 주요 동작이 한 열 또는 충분한 터치 폭을 가진 2열로 재배치되며 가로 스크롤이 없어야 한다.
- 주요 터치 대상은 최소 48px 정사각형이다.

## 5. 컴포넌트

### 상단바

- 구조: `header` 안에 홈 링크, 화면 제목, 균형용 여백.
- 상태: 기본, 호버, 눌림, 포커스.
- 접근성: 홈 링크는 명확한 이름을 가지며 제목은 해당 화면의 유일한 `h1`이다.
- 모션: 눌림은 120ms 이내의 `transform`만 사용한다.

### 활동 카드

- 구조: 아이콘, 제목, 한 문장 설명, 행동 힌트가 있는 전체 클릭 링크.
- 변형: 따라쓰기(주황), 소리 퀴즈(파랑).
- 상태: 기본, 호버, 눌림, 포커스.
- 접근성: 카드 전체가 하나의 명확한 링크이며 장식 아이콘은 숨긴다.

### 언어 선택

- 구조: `fieldset`과 2개의 버튼.
- 상태: 기본, 선택, 호버, 눌림, 포커스.
- 접근성: 현재 선택은 `aria-pressed`로 알린다.

### 동작 버튼

- 변형: 기본, 주요, 성공.
- 상태: 기본, 호버, 눌림, 포커스, 비활성.
- 접근성: 아이콘만으로 의미를 전달하지 않고 텍스트 이름을 함께 제공한다.

### 글자 선택 버튼

- 구조: 한 글자와 스크린리더용 읽기 이름.
- 상태: 기본, 선택, 정답, 오답, 포커스, 비활성.
- 배치: 375px에서 잘림 없이 줄바꿈되는 유동 그리드.

### 따라쓰기 캔버스

- 구조: 수치와 텍스트가 함께 있는 진행 표시, 안내 캔버스, 그리기 캔버스.
- 상태: 준비, 그리는 중, 완료.
- 접근성: 캔버스 목적과 현재 글자를 이름으로 제공하고 진행률은 라이브 텍스트로 보완한다.

## 6. 움직임과 상호작용

- 짧은 피드백 120ms, 일반 전환 220ms, 완료 강조 420ms.
- `transform`과 `opacity`만 애니메이션한다.
- 지속적으로 떠다니는 장식은 사용하지 않는다. 움직임은 선택, 정답, 오답, 완료처럼 의미 있는 상태에만 쓴다.
- `prefers-reduced-motion: reduce`에서는 비필수 애니메이션과 색종이 효과를 끈다.

## 7. 깊이와 표면

혼합 전략을 사용한다. 카드 자체는 부드러운 색면과 얕은 그림자로 종이 조각처럼 보이게 하고, 선택·완료처럼 중요한 상태만 테두리와 명확한 색 변화로 올린다. 내부 요소의 반경은 바깥 카드보다 작게 유지한다.

## 8. 접근성 제약과 허용된 부채

- 목표: WCAG 2.2 AA.
- 모든 상호작용 요소는 키보드로 접근 가능하고 `:focus-visible` 표시가 있어야 한다.
- 브라우저 확대를 막지 않는다.
- 색상만으로 상태를 전달하지 않고 텍스트 또는 형태를 함께 사용한다.
- 화면마다 고유하고 설명적인 제목을 제공한다.
- 한글 문구는 한 글자만 고립되는 줄바꿈을 피하고, 선택 버튼의 글자는 잘리거나 기준선이 가려지지 않아야 한다.
- 허용된 접근성 부채: 없음.
